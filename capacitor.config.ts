import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PlanFunEvents',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
